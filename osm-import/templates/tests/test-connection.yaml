apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "osm-import.fullname" . }}-test-connection"
  labels:
    {{- include "osm-import.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "osm-import.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
